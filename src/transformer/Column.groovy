package transformer

class Column {

    String title

    int length

    int padding

    Column(String title, int length, int padding) {
        this.title = title
        this.length = length
        this.padding = padding
    }
}
